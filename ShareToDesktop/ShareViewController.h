#import <UIKit/UIKit.h>

// Principal class of the ShareToDesktop app extension: no compose sheet, it
// starts uploading the shared images the moment it appears.
@interface ShareViewController : UIViewController
@end
