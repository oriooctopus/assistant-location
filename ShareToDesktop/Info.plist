<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>Share to desktop</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>XPC!</string>
	<!-- Must stay identical to the containing app's values or TestFlight
	     rejects the upload: an extension's version must match its host app.
	     The build number is rewritten for both plists by fastlane. -->
	<key>CFBundleShortVersionString</key>
	<string>1.3</string>
	<key>CFBundleVersion</key>
	<string>202405090850</string>
	<!-- Same exception the container app uses: the tailnet endpoint is plain
	     http on a private 100.x address, which ATS blocks by default. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
	</dict>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionAttributes</key>
		<dict>
			<key>NSExtensionActivationRule</key>
			<!-- This predicate decides whether iOS offers the extension at all.
			     It used to be the dictionary form with only the image and movie
			     count keys, and that form has no key for audio - so a Voice Memo
			     never showed the icon, however much the code could handle. The
			     predicate form matches on what the attachments conform to instead:
			     stills, videos, audio (Voice Memos vends com.apple.m4a-audio, which
			     conforms to public.audio), any file:// item, and any other
			     content-typed data item (Files app and mail attachments - PDFs,
			     zips - arrive as com.adobe.pdf etc, not as a file URL). A web URL
			     or a selected text snippet also conforms to public.data, so those
			     two are excluded explicitly and the icon stays hidden for them.
			     +[GLDropUploader kindOfProvider:] mirrors this rule; keep them in
			     step. The item cap is enforced here AND in ShareViewController's
			     kMaxItems; the code cap is what actually bounds memory, the
			     predicate one just keeps the sheet from offering a share the code
			     would truncate. -->
			<string>SUBQUERY($extensionItem.attachments, $attachment, ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.image" OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.movie" OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.audio" OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.file-url" OR (ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.data" AND NOT (ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.url") AND NOT (ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.text"))).@count &gt;= 1 AND $extensionItem.attachments.@count &lt;= 10</string>
		</dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>ShareViewController</string>
	</dict>
</dict>
</plist>
